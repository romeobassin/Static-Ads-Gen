{
  "name": "generate_simple_ad",
  "description": "Generate simple advertisement copy for an image",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Template identifier",
        "enum": ["simple_ad"]
      },
      "headline": {
        "type": "string",
        "description": "Main headline text for the advertisement"
      },
      "subheadline": {
        "type": "string",
        "description": "Supporting subheadline text"
      },
      "cta_text": {
        "type": "string",
        "description": "Call to action text"
      }
    },
    "required": ["id", "headline", "subheadline", "cta_text"]
  }
}